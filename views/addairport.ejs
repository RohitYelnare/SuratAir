<%- include('partials/header') %>
<link rel="stylesheet" href="css/ticket.css">

    <title>Add Airport</title>
</head>
<body>

    <div class="main">

        <!-- Sign up form -->
        
<!DOCTYPE html>
<html >
<head>
  <meta charset="UTF-8">
  
  
  
      <link rel="stylesheet" href="css/style.css">

  
</head>

<body>
    <% if(message.error) { %>
		<div class="alert alert-danger" style="text-align: center;">
			<%= message.error%>
		</div>
	<% } %>
	<% if(message.success) { %>
		<div class="alert alert-success" style="text-align: center;">
			<%= message.success %>
		</div>
	<% } %>
    
      <table class="table">
        <thead>
          <tr>
            <!-- <th scope="col">AIRPORT ID</th> -->
            <th scope="col">ICAO Code</i></th>
            <th scope="col">Airport Name</th>
            <th scope="col">City</th>
            <th scope="col">Country</th>
          </tr>
        </thead>
        <tbody>
        <%for(var i=0; i<allairports.length; i++){%>
            <tr>
                <td><%=allairports[i].code_id%></td>
                <td><%=allairports[i].name%></td>
                <td><%=allairports[i].city%></td>
                <td><%=allairports[i].country%></td>
        <%}%>
        </tbody>
      </table>
      <div class="d-flex justify-content-center">

        <form method="POST">
          <input type="text" id="airport1" placeholder="code" name="airportId" maxlength="3" style="text-transform: uppercase"> 
          <br>
          <br>
          <input type="text" id="airport1" placeholder="Airport Name" name="airportName" maxlength="70" > 
          <br>
          <br>
          <input type="text" id="airport1" placeholder="City" name="airportCity" maxlength="20" > 
          <br>
          <br>
          <input type="text" id="airport1" placeholder="Country" name="airportCountry" maxlength="50" > 
          <br>
          <br>
          <br>
          <button type="submit" id="portsubmit" name="portsubmit" class="btn btn-primary">ADD AIRPORT</button>
        </form>
      </div>
</body>
</html>
<%- include('partials/footer') %>
